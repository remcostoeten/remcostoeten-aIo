{
  "$defs": {
    "AiCompletionParameters": {
      "additionalProperties": false,
      "description": "Execution parameters for one completion.\n\n`retry_count` is accepted and validated but inert: no retry is performed.\nSampling parameters are thousandths, so 0 through 1000, and may be omitted\non the wire as well as sent explicitly as `null`.",
      "properties": {
        "maxOutputBytes": {
          "description": "Response byte budget the provider must not exceed.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "maxOutputTokens": {
          "default": null,
          "description": "Output token ceiling asked of the provider, or `null` for its default.\n\nDistinct from `max_output_bytes`, which is this side's accumulation cap\nand is enforced locally. A provider that ignores this field is not a\ncontract violation; the byte cap still applies.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "retryCount": {
          "description": "Accepted but inert. Present for wire compatibility only.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "temperatureMillis": {
          "description": "Sampling temperature in thousandths.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "timeoutMs": {
          "description": "Provider-observed timeout. This is not a service-wide deadline; see\n`docs/contracts.md` §3.2.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "topPMillis": {
          "description": "Nucleus sampling threshold in thousandths.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "maxOutputBytes",
        "timeoutMs",
        "retryCount"
      ],
      "type": "object"
    },
    "AiMessage": {
      "additionalProperties": false,
      "description": "One turn of conversation preceding the final user prompt.\n\nText only. Content parts, tool calls, attachments and names are absent\nuntil a consumer needs them; see `docs/contracts.md` §4.1.",
      "properties": {
        "content": {
          "description": "Turn text. Never empty.",
          "type": "string"
        },
        "role": {
          "$ref": "#/$defs/AiMessageRole",
          "description": "Who produced this turn."
        }
      },
      "required": [
        "role",
        "content"
      ],
      "type": "object"
    },
    "AiMessageRole": {
      "description": "Who produced a conversation turn.\n\nClosed on the wire, and deliberately without a `system` value: the system\ninstruction is [`AiCompletionRequest::system_prompt`], so a conversation\ncannot carry a second, competing instruction channel.",
      "oneOf": [
        {
          "const": "user",
          "description": "A turn from the person.",
          "type": "string"
        },
        {
          "const": "assistant",
          "description": "A turn the model previously produced.",
          "type": "string"
        }
      ]
    }
  },
  "$id": "https://schemas.ai-sdk.local/0.2.0/completion-request.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "One completion request.\n\nProvider and model identity are flat by design; a nested model reference is\na later, breaking wire change. `origin` is deliberately absent: it is an\napplication concept passed separately to the service and never sent to a\nprovider.\n\nThe conversation a provider receives is exactly\n`system_prompt`, then `prior_messages` in order, then `user_prompt` as the\nfinal user turn. `user_prompt` is the sole authority for that final turn, so\na consumer holding a `messages` array sends everything but its last entry as\n`prior_messages`.",
  "properties": {
    "modelId": {
      "description": "Provider-scoped model identity.",
      "type": "string"
    },
    "parameters": {
      "$ref": "#/$defs/AiCompletionParameters",
      "description": "Execution parameters."
    },
    "priorMessages": {
      "default": [],
      "description": "Turns preceding `user_prompt`, oldest first. Omission means none.\n\nNo alternation rule is imposed: whether consecutive same-role turns are\nmeaningful is the consuming application's judgement, and providers\ndiffer on it.",
      "items": {
        "$ref": "#/$defs/AiMessage"
      },
      "type": "array"
    },
    "providerId": {
      "description": "Identifies a registered provider instance, not a vendor globally.",
      "type": "string"
    },
    "requestId": {
      "description": "Caller-chosen identity, unique among the caller's active requests.",
      "type": "string"
    },
    "systemPrompt": {
      "description": "May be empty.",
      "type": "string"
    },
    "userPrompt": {
      "description": "The final user turn. May be empty.",
      "type": "string"
    }
  },
  "required": [
    "requestId",
    "providerId",
    "modelId",
    "systemPrompt",
    "userPrompt",
    "parameters"
  ],
  "title": "AiCompletionRequest",
  "type": "object"
}
